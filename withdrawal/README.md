# Boomerang withdrawal procedure

## Table of Contents

- [Diagrams](#diagrams)
- [Procedure](#procedure)

## Diagrams

The compact protocol-development sources are the
[initiator view](initiator_withdrawal_protocol_development.puml)
and
[non-initiator view](non_initiator_withdrawal_protocol_development.puml).
They show causal gates, role differences, repeated digging behavior, and
Boomlet state transitions. Their named message and acceptance contracts are
defined in
[withdrawal_development_contracts.md](withdrawal_development_contracts.md).

The canonical sequences with prose comments removed are the
[initiator no-prose source](initiator_withdrawal_diagram_without_states_without_prose_comments.puml)
and
[non-initiator no-prose source](non_initiator_withdrawal_diagram_without_states_without_prose_comments.puml).
Their compact guard calls are defined in
[withdrawal_no_prose_guards.md](withdrawal_no_prose_guards.md), and their
cryptographic calls are defined in
[no_prose_crypto_contracts.md](../no_prose_crypto_contracts.md).

The detailed graphical sequence diagrams are the
[initiator SVG](initiator_withdrawal_diagram_without_states.svg) and
[non-initiator SVG](non_initiator_withdrawal_diagram_without_states.svg).
The development views, contract tables, detailed diagrams, and this README are
explanatory implementation aids. Normative protocol requirements are defined
only by [SPEC.md](../spec/SPEC.md).

## Procedure

Withdrawal role notation: `peer_0` in the diagrams names the initiator role for
the expanded example, not fixed setup peer index 0. Approval sets are
canonicalized in active setup peer order by `peer_ids_collection`.

1. Niso receives psbt from User. Niso checks that most_work_bitcoin_block_height has passed milestone_block_0 from boomerang_descriptor, derives tx_id from psbt and stores it. Niso also checks to see if psbt inputs are satisfiable. It sends the psbt alongside the latest block_height to Boomlet.
2. Boomlet receives {psbt, niso_0_event_block_height} from Niso and checks to see if the niso_0_event_block_height has passed milestone_block_0. Boomlet then derives tx_id from psbt, creates st_preview_nonce, uses st_preview_nonce for ST freshness, encrypts it for st_0_identity_pubkey and sends the envelope to Niso.
3. Niso receives tx_id_with_nonce_encrypted_by_boomlet_0_for_st from Boomlet and forwards it to ST.
4. ST receives tx_id_with_nonce_encrypted_by_boomlet_0_for_st from Niso, decrypts it, separates the nonce and sends the tx_id to User.
5. User receives tx_id from ST and checks to see if it matches that of the psbt he generated. User then informs ST of his acknowledgement of tx_id.
6. ST receives {magic: "withdrawal_initiator_peer_approved_that_txid_received_is_the_same_as_the_one_derived_from_withdrawal_psbt"} from User. ST then signs the tx_id_with_nonce it received with st_privkey and encrypts it with boomlet_0_identity_pubkey. It then sends this encrypted message to Niso.
7. Niso receives tx_id_with_nonce_signed_by_st_encrypted_by_st_for_boomlet_0 from ST and forwards it to Boomlet.
8. Boomlet receives tx_id_with_nonce_signed_by_st_encrypted_by_st_for_boomlet_0 from Niso, decrypts it, verifies st_0_identity_pubkey's signature and checks if the content is the same as the one sent i.e. User has acknowledged the tx_id and the message is fresh. Boomlet then creates initiator_approval_nonce and computes withdrawal_id. It then generates peer_0_tx_approval containing withdrawal_id, initiator_approval_nonce, and event_block_height, signs it with boomlet_0_identity_privkey and encrypts it for wt. It then encrypts psbt for every other boomlet_i using withdrawal_id-bound envelopes. Boomlet sends withdrawal_id, the resulting psbt_encrypted_collection, and peer_0_tx_approval_signed_by_boomlet_0_encrypted_by_boomlet_0_for_wt to Niso.
9. Niso receives {withdrawal_id, peer_0_tx_approval_signed_by_boomlet_0_encrypted_by_boomlet_0_for_wt, psbt_encrypted_collection: Collection<psbt_encrypted_by_boomlet_0_for_boomlet_i> [1 <= i <= 4]} from Boomlet and forwards them to WT.
10. WT receives {withdrawal_id, peer_0_tx_approval_signed_by_boomlet_0_encrypted_by_boomlet_0_for_wt, psbt_encrypted_collection: Collection<psbt_encrypted_by_boomlet_0_for_boomlet_i> [1 <= i <= 4]} from Niso. WT decrypts peer_0_tx_approval_signed_by_boomlet_0_encrypted_by_boomlet_0_for_wt using canonical context {"withdrawal_tx_approval", withdrawal_id}, verifies boomlet_0_identity_pubkey's signature, checks that the signed peer_0_tx_approval carries the same withdrawal_id, and checks that the event_block_height is fresh. Then it constructs its own wt_tx_approval containing withdrawal_id and most_work_bitcoin_block_height and signs it. It sends to each non-initiator peer wt_tx_approval_signed_by_wt, peer_0_tx_approval_signed_by_boomlet_0 and psbt encrypted by that peer's boomlet_identity_pubkey.
11. Non-initiator Niso receives {wt_tx_approval_signed_by_wt, peer_0_tx_approval_signed_by_boomlet_0, psbt_encrypted_by_boomlet_0_for_boomlet_1} from WT. Non-initiator Niso verifies wt_pubkey's signature on wt_tx_approval_signed_by_wt and verifies boomlet_0_identity_pubkey's signature on peer_0_tx_approval_signed_by_boomlet_0. It checks that boomlet_0_identity_pubkey exists in the set of peer_ids_collection. Non-initiator Niso checks that the withdrawal_id values match, peer_0_tx_approval is fresh according to wt_tx_approval, and wt_tx_approval is fresh according to most_work_bitcoin_block_height. It then checks that most_work_bitcoin_block_height has passed milestone_block_0 from boomerang_descriptor and forwards the received message alongside niso_1_event_block_height to Non-initiator Boomlet.
12. Non-initiator Boomlet receives {wt_tx_approval_signed_by_wt, peer_0_tx_approval_signed_by_boomlet_0, psbt_encrypted_by_boomlet_0_for_boomlet_1, niso_1_event_block_height} from Non-initiator Niso. Non-initiator Boomlet verifies wt_pubkey's signature on wt_tx_approval_signed_by_wt and verifies boomlet_0_identity_pubkey's signature on peer_0_tx_approval_signed_by_boomlet_0. It verifies that boomlet_0_identity_pubkey exists in the set of peer_ids_collection, decrypts psbt_encrypted_by_boomlet_0_for_boomlet_1 using boomlet_1_identity_privkey and boomlet_0_identity, and reconstructs withdrawal_id from setup_instance_id, psbt.derive_tx_id(), boomlet_0_identity_pubkey, and peer_0_tx_approval.approval_nonce. Non-initiator Boomlet checks that the reconstructed withdrawal_id matches peer_0_tx_approval and wt_tx_approval, that both approvals are fresh, and that milestone_block_0 has passed. Non-initiator Boomlet then sends psbt to non-initiator Niso.
13. Non-initiator Niso receives psbt from Non-initiator Boomlet. Non-initiator Niso checks that psbt is satisfiable and that the derived tx_id reconstructs peer_0_tx_approval.withdrawal_id under the active setup. It then outputs psbt along with initiator's peer_i_id to non-initiator User.
14. Non-initiator User receives {psbt, initiator_peer_id} from non-initiator Niso. Non-initiator User then informs non-initiator Niso of its agreement with the tx.
15. Non-initiator Niso receives {magic: "withdrawal_non_initiator_peer_approved_the_withdrawal_psbt"} from non-initiator User. Non-initiator Niso then informs non-initiator Boomlet to proceed.
16. Non-initiator Boomlet receives {magic: "withdrawal_peer_agreement_with_psbt_received"} from non-initiator Niso. Non-initiator Boomlet then derives tx_id from psbt, adds st_preview_nonce to it and encrypts it with st_1_identity_pubkey. It then sends tx_id_with_nonce_encrypted_by_boomlet_1_for_st to non-initiator Niso.
17. Non-initiator Niso receives tx_id_with_nonce_encrypted_by_boomlet_1_for_st from non-initiator Boomlet and forwards it to non-initiator ST.
18. Non-initiator ST receives tx_id_with_nonce_encrypted_by_boomlet_1_for_st from non-initiator Niso. Non-initiator ST decrypts tx_id_with_nonce_encrypted_by_boomlet_1_for_st to get tx_id_with_nonce and outputs the content of it i.e. tx_id to non-initiator User.
19. Non-initiator User receives tx_id from non-initiator ST. Non-initiator User checks to see tx_id matches the one he previously received from non-initiator Niso and agreed to. It then informs non-initiator ST of his agreement.
20. Non-initiator ST receives {magic: "withdrawal_non_initiator_peer_approved_that_txid_received_is_the_same_as_the_one_derived_from_withdrawal_psbt"} from non-initiator User. It signs the tx_id_with_nonce, encrypts it with boomlet_1_identity_pubkey and sends the result to non-initiator Niso.
21. Non-initiator Niso receives tx_id_with_nonce_signed_by_st_encrypted_by_st_for_boomlet_1 from non-initiator ST. Non-initiator Niso forwards tx_id_with_nonce_signed_by_st_encrypted_by_st_for_boomlet_1 along with the updated niso_1_event_block_height to non-initiator Boomlet.
22. Non-initiator Boomlet receives {tx_id_with_nonce_signed_by_st_encrypted_by_st_for_boomlet_1, niso_1_event_block_height} from non-initiator Niso. Non-initiator Boomlet decrypts tx_id_with_nonce_signed_by_st_encrypted_by_st_for_boomlet_1 using boomlet_1_identity_privkey to get tx_id_with_nonce_signed_by_st. It then verifies st_1_identity_pubkey's signature on tx_id_with_nonce_signed_by_st and checks if the tx_id_with_nonce received matches with the one sent. Non-initiator Boomlet generates peer_1_tx_approval containing withdrawal_id, approval_nonce, and niso_1_event_block_height, signs it using boomlet_1_identity_privkey, encrypts it with wt_pubkey and sends withdrawal_id with peer_1_tx_approval_signed_by_boomlet_1_encrypted_by_boomlet_1_for_wt to non-initiator Niso.
23. Non-initiator Niso receives {withdrawal_id, peer_1_tx_approval_signed_by_boomlet_1_encrypted_by_boomlet_1_for_wt} from non-initiator Boomlet and forwards them to WT.
24. WT receives {withdrawal_id, peer_1_tx_approval_signed_by_boomlet_1_encrypted_by_boomlet_1_for_wt} from non-initiator Niso, decrypts it using canonical context {"withdrawal_tx_approval", withdrawal_id}, verifies boomlet_1_identity_pubkey's signature on peer_1_tx_approval_signed_by_boomlet_1, checks that withdrawal_id matches the expected value, and checks that the message is fresh according to the most_work_bitcoin_block_height and event_block_height of wt_tx_approval. It also receives withdrawal_id with peer_i_tx_approval_signed_by_boomlet_i_encrypted_by_boomlet_i_for_wt from non-initiator Peers and repeats the same validation process. WT sorts the valid approvals into ordered_peer_tx_approvals in active setup peer order and derives ordered_non_initiator_peer_tx_approvals by omitting the initiator's approval. WT then sends ordered_non_initiator_peer_tx_approvals to each non-initiator peer and in a separate message sends ordered_peer_tx_approvals along with wt_tx_approval to the initiator peer.
25. WT collects one valid peer_i_tx_approval_signed_by_boomlet_i from every active setup peer, sorts the valid approvals into ordered_peer_tx_approvals in active setup peer order, and derives ordered_non_initiator_peer_tx_approvals by omitting the initiator's approval. Each receiver verifies this WT-supplied ordering before computing approved_withdrawal_id.
26. Niso receives {ordered_peer_tx_approvals, wt_tx_approval_signed_by_wt} from WT. Niso verifies each boomlet_i_identity_pubkey's signature on each peer_i_tx_approval_signed_by_boomlet_i and verifies wt_pubkey's signature on wt_tx_approval_signed_by_wt. It checks withdrawal_id to match what was sent, the event_block_heights to be in the appropriate range, and ordered_peer_tx_approvals to contain exactly one approval per active setup peer in peer_ids_collection order. It computes approved_withdrawal_id from withdrawal_id and ordered_peer_tx_approvals for local tracking, then forwards ordered_peer_tx_approvals with the latest block to Boomlet.

    26n. Non-initiator Niso receives {ordered_non_initiator_peer_tx_approvals} from WT. Non-initiator Niso updates niso_1_event_block_height with the latest most_work_bitcoin_block_height. For every active setup peer except the initiator, it verifies boomlet_i_identity_pubkey's signature on peer_i_tx_approval_signed_by_boomlet_i, checks withdrawal_id to be the expected value, peer_i_tx_approval to be fresh according to niso_1_event_block_height and wt_tx_approval, and checks that wt_tx_approval is still fresh. It verifies the subset is ordered by peer_ids_collection with the initiator omitted, reconstructs ordered_peer_tx_approvals by inserting the stored initiator approval at the initiator's active setup peer position, and computes approved_withdrawal_id from the complete ordered set for local tracking. It forwards ordered_non_initiator_peer_tx_approvals alongside niso_1_event_block_height to non-initiator Boomlet.
27. Boomlet receives {ordered_peer_tx_approvals, wt_tx_approval_signed_by_wt, niso_0_event_block_height} from Niso. Boomlet verifies each boomlet_i_identity_pubkey's signature on each peer_i_tx_approval_signed_by_boomlet_i, verifies wt_pubkey's signature on wt_tx_approval_signed_by_wt and updates niso_0_event_block_height with the new value. It checks withdrawal_id to match what was sent, the event_block_heights to be in the appropriate range, and ordered_peer_tx_approvals to contain exactly one approval per active setup peer in peer_ids_collection order. It computes approved_withdrawal_id from withdrawal_id and ordered_peer_tx_approvals. It then generates duress_check_space as one randomly sorted list of integers from 1 to 193, including all elements of 1..193, adds a nonce to it and encrypts it. Then Boomlet sends duress_check_space_with_nonce_encrypted_by_boomlet_0_for_st to Niso.

    27n. Non-initiator Boomlet receives {ordered_non_initiator_peer_tx_approvals, niso_1_event_block_height} from non-initiator Niso. After renewing niso_1_event_block_height, for every peer in peer_ids_collection except the initiator, non-initiator Boomlet verifies boomlet_i_identity_pubkey's signature on peer_i_tx_approval_signed_by_boomlet_i, checks withdrawal_id to be the expected value, peer_i_tx_approval to be fresh according to niso_1_event_block_height and wt_tx_approval, and checks that wt_tx_approval is still fresh. It verifies the subset is ordered by peer_ids_collection with the initiator omitted, reconstructs ordered_peer_tx_approvals by inserting the stored initiator approval at the initiator's active setup peer position, and computes approved_withdrawal_id from withdrawal_id and the complete ordered set. It then generates duress_check_space as one randomly sorted list of integers from 1 to 193, including all elements of 1..193, adds a nonce to it and encrypts it. Then non-initiator Boomlet sends duress_check_space_with_nonce_encrypted_by_boomlet_1_for_st to non-initiator Niso.
28. Niso receives duress_check_space_with_nonce_encrypted_by_boomlet_0_for_st from Boomlet and forwards it to ST.

    28n. Non-initiator Niso receives duress_check_space_with_nonce_encrypted_by_boomlet_1_for_st from non-initiator Boomlet and forwards it to non-initiator ST.
29. ST receives duress_check_space_with_nonce_encrypted_by_boomlet_0_for_st from Niso, decrypts it to get duress_check_space_with_nonce, maps each integer in duress_check_space through DURESS_DISPLAY_VOCABULARY, creates 5 independently shuffled display columns from the single received list, and keeps display-to-original-index mappings locally.

    29n. Non-initiator ST receives duress_check_space_with_nonce_encrypted_by_boomlet_1_for_st from non-initiator Niso, decrypts it to get duress_check_space_with_nonce, maps each integer in duress_check_space through DURESS_DISPLAY_VOCABULARY, creates 5 independently shuffled display columns from the single received list, and keeps display-to-original-index mappings locally.
30. User receives 5 independently shuffled columns from ST. If he is not being coerced he will select 1 country in each column to replicate the duress_consent_set and inputs it to ST.

    30n. Non-initiator User receives 5 independently shuffled columns from non-initiator ST. If he is not being coerced he will select 1 country in each column to replicate the duress_consent_set and inputs it to ST.
31. ST receives the displayed selections from User, converts them into indices in Boomlet's original single list, then creates duress_signal_index_with_nonce using the nonce from previously received duress_check_space_with_nonce, encrypts it for Boomlet and sends it to Niso.

    31n. Non-initiator ST receives the displayed selections from non-initiator User, converts them into indices in Boomlet's original single list, then creates duress_signal_index_with_nonce using the nonce from previously received duress_check_space_with_nonce, encrypts it for non-initiator Boomlet and sends it to Niso.
32. Niso receives duress_signal_index_with_nonce_encrypted_by_st_for_boomlet_0 from ST and forwards it to Boomlet.

    32n. Non-initiator Niso receives duress_signal_index_with_nonce_encrypted_by_st_for_boomlet_1 from non-initiator ST and forwards it to non-initiator Boomlet.
33. Boomlet receives duress_signal_index_with_nonce_encrypted_by_st_for_boomlet_0 from Niso. Boomlet rejects no outstanding duress check, wrong phase, duplicate response, wrong nonce, wrong count, duplicate indices, or out-of-range indices. If valid, Boomlet resolves the returned original-list indices against the single list in duress_check_space assigning the resulting integers to duress_signal. If the duress_signal is composed of the same set of numbers as the duress_consent_set, duress_placeholder_plaintext will be assigned all-zero 32-byte padding otherwise it will be the doxing_key_for_sar. duress_placeholder_plaintext will be encrypted to make up an approved_withdrawal_id-bound duress_placeholder. Boomlet then constructs peer_0_tx_commit containing approved_withdrawal_id and niso_0_event_block_height, signs it, pads it with duress_placeholder, re-signs it and encrypts it with wt_pubkey and sends the result to Niso.

    33n. Non-initiator Boomlet receives duress_signal_index_with_nonce_encrypted_by_st_for_boomlet_1 from non-initiator Niso. Non-initiator Boomlet rejects no outstanding duress check, wrong phase, duplicate response, wrong nonce, wrong count, duplicate indices, or out-of-range indices. If valid, it resolves the returned original-list indices against the single list in duress_check_space assigning the resulting integers to duress_signal. If duress_signal is composed of the same set of numbers as the duress_consent_set, duress_placeholder_plaintext will be assigned an all-zero 32-byte padding otherwise it will be the doxing_key_for_sar. duress_placeholder_plaintext will be encrypted to make up duress_placeholder. Non-initiator Boomlet then computes an approval-set attestation fingerprint from ordered_peer_tx_approvals and wt_tx_approval_signed_by_wt after verifying them, signs the fingerprint with boomlet_1_identity_privkey, and sends it to non-initiator Niso. The attestation means only that it received and verified the complete approval set and computed the same approved_withdrawal_id.
34. Niso receives peer_0_tx_commit_signed_by_boomlet_0_padded_signed_by_boomlet_0_encrypted_by_boomlet_0_for_wt from Boomlet and forwards it to WT.

    34n. Non-initiator Niso receives approval_set_attestation_fingerprint_signed_by_boomlet_1 from non-initiator Boomlet and forwards it to WT.
35. WT receives peer_0_tx_commit_signed_by_boomlet_0_padded_signed_by_boomlet_0_encrypted_by_boomlet_0_for_wt from Niso. It decrypts the message, verifies boomlet_0_identity_pubkey's signature, extracts duress_placeholder and peer_0_tx_commit_signed_by_boomlet_0, and stages them. It does not forward the placeholder to SAR until it has verified the four non-initiator approval-set attestations.

    35n. WT receives approval_set_attestation_fingerprint_signed_by_boomlet_1 from non-initiator Niso. WT recomputes approval_set_attestation_fingerprint from ordered_peer_tx_approvals and wt_tx_approval_signed_by_wt, verifies boomlet_1_identity_pubkey's signature, and requires the signed content to equal the recomputed fingerprint. At the same time it receives approval_set_attestation_fingerprint_signed_by_boomlet_i [2 <= i <= 4] and verifies exactly one matching attestation from each non-initiator Boomlet. Each attestation means only that its signer received and verified the complete approval set and computed the same approved_withdrawal_id. Together with the matching approved_withdrawal_id in the staged initiator commit, the four attestations confirm that all five Boomlets received the complete approval set. After all four verify, WT may forward the staged initiator placeholder to SAR.
36. SAR receives {duress_placeholder, boomlet_0_identity_pubkey, approved_withdrawal_id} from WT. It decrypts duress_placeholder using the Boomlet-to-SAR CBC/CMAC envelope context and sar_privkey to get duress_placeholder_plaintext. It classifies the plaintext as valid safe if it is all-zero 32-byte padding, valid duress if its derived doxing_data_identifier exists, or malformed otherwise. If it was valid duress and the tuple of approved_withdrawal_id, the received boomlet_0_identity_pubkey, and envelope IV of duress_placeholder was not previously saved, it uses duress_placeholder_plaintext which is equal to doxing_key_for_sar to decrypt the associated static_doxing_data_encrypted_by_doxing_key_for_sar and dynamic_doxing_data_encrypted_by_doxing_key_for_sar. It then uses this doxing_data to enter SEARCH AND RESCUE. For valid safe or duress placeholders, SAR signs duress_placeholder with sar_privkey and encrypts it with boomlet_0_identity_pubkey. Malformed placeholders are rejected before any acknowledgment is created.
37. WT receives duress_placeholder_signed_by_sar_encrypted_by_sar_for_boomlet_0 from SAR and continues processing peer_0_tx_commit_signed_by_boomlet_0. It verifies boomlet_0_identity_pubkey's signature on peer_0_tx_commit_signed_by_boomlet_0, checks approved_withdrawal_id to be the one expected and event_block_height to be fresh. WT then signs peer_0_tx_commit_signed_by_boomlet_0 with wt_privkey and sends it to non-initiator peers. WT receives each peer i's peer_i_tx_commit_signed_by_boomlet_i in response and checks its correctness in a similar manner. WT then sends the collection of peer_i_tx_commit_signed_by_boomlet_i to Niso alongside duress_placeholder_signed_by_sar_encrypted_by_sar_for_boomlet_0.
38. Non-initiator Niso receives peer_0_tx_commit_signed_by_boomlet_0_signed_by_wt from WT. Non-initiator Niso updates niso_1_event_block_height with the most_work_bitcoin_block_height, verifies wt_pubkey's signature on peer_0_tx_commit_signed_by_boomlet_0_signed_by_wt, boomlet_0_identity_pubkey's signature on peer_0_tx_commit_signed_by_boomlet_0, approved_withdrawal_id to be the expected value, and event_block_height to be fresh according to most_work_bitcoin_block_height. Non-initiator Niso then forwards peer_0_tx_commit_signed_by_boomlet_0_signed_by_wt alongside niso_1_event_block_height to Non-initiator Boomlet.
39. Non-initiator Boomlet receives {peer_0_tx_commit_signed_by_boomlet_0_signed_by_wt, niso_1_event_block_height} from non-initiator Niso. Non-initiator Boomlet verifies wt_pubkey's signature on peer_0_tx_commit_signed_by_boomlet_0_signed_by_wt, boomlet_0_identity_pubkey's signature on peer_0_tx_commit_signed_by_boomlet_0, approved_withdrawal_id to be the expected value, and event_block_height to be fresh according to niso_1_event_block_height. Non-initiator Boomlet then generates peer_1_tx_commit containing approved_withdrawal_id and event_block_height, signs it with boomlet_1_identity_privkey, pads it with duress_placeholder, signs it again and encrypts it with wt_pubkey. It then sends peer_1_tx_commit_signed_by_boomlet_1_padded_signed_by_boomlet_1_encrypted_by_boomlet_1_for_wt to Niso.
40. Non-initiator Niso receives peer_1_tx_commit_signed_by_boomlet_1_padded_signed_by_boomlet_1_encrypted_by_boomlet_1_for_wt from non-initiator Boomlet and forwards it to WT.
41. WT receives peer_1_tx_commit_signed_by_boomlet_1_padded_signed_by_boomlet_1_encrypted_by_boomlet_1_for_wt from non-initiator Niso, decrypts it with wt_privkey, verifies boomlet_1_identity_pubkey's signature. It then derives duress_placeholder by separating the padding from peer_1_tx_commit_signed_by_boomlet_1_padded and sends it to SAR. It also saves peer_1_tx_commit_signed_by_boomlet_1 for later.
42. SAR receives {duress_placeholder, boomlet_1_identity_pubkey, approved_withdrawal_id} from WT. It decrypts duress_placeholder using the Boomlet-to-SAR CBC/CMAC envelope context and sar_privkey to get duress_placeholder_plaintext. It classifies the plaintext as valid safe if it is all-zero 32-byte padding, valid duress if its derived doxing_data_identifier exists, or malformed otherwise. If it was valid duress and the tuple of approved_withdrawal_id, the received boomlet_1_identity_pubkey, and envelope IV of duress_placeholder was not previously saved, it uses duress_placeholder_plaintext which is equal to doxing_key_for_sar to decrypt the associated static_doxing_data_encrypted_by_doxing_key_for_sar and dynamic_doxing_data_encrypted_by_doxing_key_for_sar. It then uses this doxing_data to enter SEARCH AND RESCUE. For valid safe or duress placeholders, SAR signs duress_placeholder with sar_privkey and encrypts it with boomlet_1_identity_pubkey. Malformed placeholders are rejected before any acknowledgment is created.
43. WT receives duress_placeholder_signed_by_sar_encrypted_by_sar_for_boomlet_1 from SAR and continues processing peer_1_tx_commit_signed_by_boomlet_1. It verifies boomlet_1_identity_pubkey's signature on peer_1_tx_commit_signed_by_boomlet_1, checks approved_withdrawal_id to be the one expected and event_block_height to be fresh. WT then signs peer_1_tx_commit_signed_by_boomlet_1 with wt_privkey. WT then sends the collection of peer_i_tx_commit_signed_by_boomlet_i_signed_by_wt [0 <= i <= 4] to Each Niso alongside its respective duress_placeholder_signed_by_sar_encrypted_by_sar_for_boomlet_i.
44. Niso receives {Collection<peer_i_tx_commit_signed_by_boomlet_i_signed_by_wt> [0 <= i <= 4], duress_placeholder_signed_by_sar_encrypted_by_sar_for_boomlet_0} from WT. It then verifies wt_pubkey's signature on peer_i_tx_commit_signed_by_boomlet_i_signed_by_wt, each non-initiator peer i's boomlet_i_identity_pubkey's signature on peer_i_tx_commit_signed_by_boomlet_i, approved_withdrawal_id to be the one expected, and event_block_height to be fresh. It then forwards Collection<peer_i_tx_commit_signed_by_boomlet_i_signed_by_wt> [1 <= i <= 4], duress_placeholder_signed_by_sar_encrypted_by_sar_for_boomlet_0 and the latest block height it sees as niso_0_event_block_height to Boomlet.
45. Boomlet receives {Collection<peer_i_tx_commit_signed_by_boomlet_i_signed_by_wt> [0 <= i <= 4], niso_0_event_block_height, duress_placeholder_signed_by_sar_encrypted_by_sar_for_boomlet_0} from Niso. It first decrypts duress_placeholder_signed_by_sar_encrypted_by_sar_for_boomlet_0, verifies sar_pubkey's signature on duress_placeholder_signed_by_sar and checks to see if the content matches of the duress_placeholder that it had sent to WT. It then verifies wt_pubkey's signature on peer_i_tx_commit_signed_by_boomlet_i_signed_by_wt, each non-initiator peer i's boomlet_i_identity_pubkey's signature on peer_i_tx_commit_signed_by_boomlet_i, approved_withdrawal_id to be the one expected, and event_block_height to be fresh according to niso_0_event_block_height. After that Boomlet draws a fresh ceremony mystery, initializes counter with 0, reached_boomlets_collection with an empty set, ping_seq_num with 0, reached_mystery_flag with 0, sets last_seen_block to niso_0_event_block_height. Boomlet then constructs Ping containing approved_withdrawal_id, last_seen_block, ping_seq_num, and reached_mystery_flag, signs it with boomlet_0_identity_privkey, freshly re-encrypts the current duress_placeholder_plaintext for SAR, pads the ping with this new duress_placeholder, signs peer_0_ping_signed_by_boomlet_0_padded with boomlet_0_identity_privkey, encrypts the result with wt_pubkey and sends peer_0_ping_signed_by_boomlet_0_padded_signed_by_boomlet_0_encrypted_by_boomlet_0_for_wt to Niso.
46. Niso receives peer_0_ping_signed_by_boomlet_0_padded_signed_by_boomlet_0_encrypted_by_boomlet_0_for_wt from Boomlet and forwards it to WT.
47. WT receives peer_0_ping_signed_by_boomlet_0_padded_signed_by_boomlet_0_encrypted_by_boomlet_0_for_wt from Niso (and peer_i_ping_signed_by_boomlet_i_padded_signed_by_boomlet_i_encrypted_by_boomlet_i_for_wt from Peers). If this is the first ping received, WT initializes reached_pings_collection with an empty set and goes into a loop that ends when reached_pings_collection[peer_i_id] has a valid value for every peer. WT decrypts peer_i_ping_signed_by_boomlet_i_padded_signed_by_boomlet_i and verifies boomlet_i_identity_pubkey's signature. Wt then separates duress_placeholder, which is the padding, and peer_i_ping. This diagram expands only peer 0's SAR path: WT sends peer 0's duress_placeholder and boomlet_0_identity_pubkey to peer 0's SAR. Other peers' placeholders are routed to their own setup-bound SARs in the corresponding peer flows.
48. SAR receives {duress_placeholder, boomlet_0_identity_pubkey, approved_withdrawal_id} from WT. It decrypts duress_placeholder using the Boomlet-to-SAR CBC/CMAC envelope context and sar_privkey to get duress_placeholder_plaintext. It classifies the plaintext as valid safe if it is all-zero 32-byte padding, valid duress if its derived doxing_data_identifier exists, or malformed otherwise. If it was valid duress and the tuple of approved_withdrawal_id, the received boomlet_0_identity_pubkey, and envelope IV of duress_placeholder was not previously saved, it uses duress_placeholder_plaintext which is equal to doxing_key_for_sar to decrypt the associated static_doxing_data_encrypted_by_doxing_key_for_sar and dynamic_doxing_data_encrypted_by_doxing_key_for_sar. It then uses this doxing_data to enter SEARCH AND RESCUE. For valid safe or duress placeholders, SAR signs duress_placeholder with sar_privkey and encrypts it with boomlet_0_identity_pubkey. Malformed placeholders are rejected before any acknowledgment is created.
49. WT receives duress_placeholder_signed_by_sar_encrypted_by_sar_for_boomlet_0 from SAR and continues processing peer_0_ping_signed_by_boomlet_0 and peer_i_ping_signed_by_boomlet_i. It verifies boomlet_i_identity_pubkey's signature on peer_i_ping_signed_by_boomlet_i and checks approved_withdrawal_id to be the one expected. If this is not the first ping it also checks that ping_seq_num has increased.  If reached_mystery_flag is 1, WT assigns peer_i_ping_signed_by_boomlet_i to reached_pings_collection[peer_i_id]. WT constructs pong containing approved_withdrawal_id, most_work_bitcoin_block_height, and Collection<peer_j_ping_signed_by_boomlet_j> [for each Peer i's pong include peer_j_ping_signed_by_boomlet_j of all other peers (i != j)], if it isn't the first pong composed WT waits for at least 1 block to pass. WT signs the pong and encrypts it for Boomlet to get pong_i_signed_by_wt_encrypted_by_wt_for_boomlet_i. It then sends pong_0_signed_by_wt_encrypted_by_wt_for_boomlet_0 alongside duress_placeholder_signed_by_sar_encrypted_by_sar_for_boomlet_0 to Niso and pong_i_signed_by_wt_encrypted_by_wt_for_boomlet_i [1 <= i <= 4] to other peers.
50. Niso receives {pong_0_signed_by_wt_encrypted_by_wt_for_boomlet_0, duress_placeholder_signed_by_sar_encrypted_by_sar_for_boomlet_0} from WT and forwards them to Boomlet alongside the latest most_work_bitcoin_block_height as niso_0_event_block_height.
51. Boomlet receives {pong_0_signed_by_wt_encrypted_by_wt_for_boomlet_0, niso_0_event_block_height, duress_placeholder_signed_by_sar_encrypted_by_sar_for_boomlet_0} from Niso. It first decrypts duress_placeholder_signed_by_sar_encrypted_by_sar_for_boomlet_0, verifies sar_pubkey's signature on duress_placeholder_signed_by_sar and checks to see if the content matches of the duress_placeholder that it had sent to WT. It then decrypts pong_0_signed_by_wt_encrypted_by_wt_for_boomlet_0 to get pong_0_signed_by_wt and verifies wt_pubkey's signature. Boomlet then checks approved_withdrawal_id to match the expected value and pong to be fresh according to niso_0_event_block_height. It then takes out prev_ping_i_signed_by_boomlet_i from pong_0 for every other peer i, verifies boomlet_i_identity_pubkey's signature, checks approved_withdrawal_id is the one expected, if it's not the first ping received the ping_seq_num has increased, if reached_boomlets_collection[peer_i_id] was previously assigned a value reached_mystery_flag is not 0. At this moment Boomlet draws an unbiased duress_check_draw := random_integer(1, DURESS_CHECK_INTERVAL_IN_BLOCKS). If duress_check_draw == 1, Boomlet enters into a duress_check to update duress_placeholder_plaintext. Otherwise it keeps the current duress_placeholder_plaintext, but the next ping still uses a freshly encrypted duress_placeholder.
52. Niso receives duress_check_space_with_nonce_encrypted_by_boomlet_0_for_st from Boomlet and forwards it to ST.
53. ST receives duress_check_space_with_nonce_encrypted_by_boomlet_0_for_st from Niso, decrypts it to get duress_check_space_with_nonce, maps each integer in duress_check_space through DURESS_DISPLAY_VOCABULARY, creates 5 independently shuffled display columns from the single list, and keeps display-to-original-index mappings locally.
54. User receives 5 independently shuffled columns from ST. If he is not being coerced he will select 1 country in each column to replicate the duress_consent_set and inputs it to ST.
55. ST receives the displayed selections from User, converts them into indices in Boomlet's original single list, then creates duress_signal_index_with_nonce using the nonce from previously received duress_check_space_with_nonce, encrypts it for Boomlet and sends it to Niso.
56. Niso receives duress_signal_index_with_nonce_encrypted_by_st_for_boomlet_0 from ST and forwards it to Boomlet.
57. Boomlet receives duress_signal_index_with_nonce_encrypted_by_st_for_boomlet_0 from Niso. Boomlet rejects no outstanding duress check, wrong phase, duplicate response, wrong nonce, wrong count, duplicate indices, or out-of-range indices. If valid, Boomlet resolves the returned original-list indices against the single list in duress_check_space assigning the resulting integers to duress_signal. If the duress_signal is composed of the same set of numbers as the duress_consent_set, duress_placeholder_plaintext will be assigned all-zero 32-byte padding otherwise it will be doxing_key_for_sar. duress_placeholder_plaintext is updated; the next ping re-encrypts the current plaintext after this optional check.

    Regardless of whether a new duress procedure ran, Boomlet freshly encrypts duress_placeholder_plaintext for SAR. Counter advancement is evaluated before updating last_seen_block and occurs only if the local chain view advanced and every other peer's prev_ping_i.last_seen_block is within the accepted boundary around the current local Niso height. Independently of counter advancement, every otherwise valid Pong then updates last_seen_block by at most JUMP_IN_BLOCKS_IF_LAST_SEEN_BLOCK_LAGS_BEHIND_NISO_EVENT_BLOCK_HEIGHT_IN_BOOMLET toward niso_0_event_block_height. It records monotonic reached flags and sets its own reached_mystery_flag once counter reaches mystery, then generates a fresh Ping with incremented ping_seq_num.
58. Niso receives peer_0_ping_signed_by_boomlet_0_padded_signed_by_boomlet_0_encrypted_by_boomlet_0_for_wt from Boomlet and forwards it to WT.
59. WT receives peer_0_ping_signed_by_boomlet_0_padded_signed_by_boomlet_0_encrypted_by_boomlet_0_for_wt from Niso and jumps back to the beginning of the PING_PONG_LOOP. if the loop breaks WT sends its reached_pings_collection to all Peers.
60. Niso receives reached_pings_collection from WT. It verifies that the collection contains exactly one current reached ping from every active peer, in active setup peer order, with valid signatures, expected approved_withdrawal_id, current ping sequence, fresh last_seen_block, and reached_mystery_flag set. Only after those checks does Niso hydrate the PSBT without changing the transaction semantics or tx_id, then sends reached_pings_collection and hydrated_psbt to Boomlet.
61. Boomlet receives {reached_pings_collection, hydrated_psbt} from Niso. Boomlet independently verifies that every active peer has exactly one current reached ping for approved_withdrawal_id, with current ping sequence, fresh last_seen_block, and reached_mystery_flag set. It then verifies the hydrated PSBT preserves the committed tx_id and remains spendable by the Boomerang descriptor before entering signing.
62. Niso receives {magic: "withdrawal_ready_to_sign"} from Boomlet and proceeds to inform User to connect Boomlet to Iso.
63. User receives {magic: "withdrawal_ready_to_sign_received_connect_boomlet_to_iso"} from Niso and proceeds to connect Boomlet to Iso and input network, account, key_index, mnemonic, and passphrase to Iso.
64. Iso receives {network, account, key_index, mnemonic, passphrase} from User and reconstructs normal_privkey_0 at m/52102'/coin_type'/account'/0/key_index using the same values as setup. It derives normal_pubkey_0 and informs Boomlet that MuSig2 signing can start for the PSBT inputs.
65. Boomlet receives {magic: "withdrawal_initialized_start_signing"} from Iso and proceeds to share with it the information necessary for signing i.e. psbt, boomerang_descriptor, boomlet_0_boom_musig2_pubkey_share, alongside his own public nonce named pubnonce_boom.
66. Iso receives {psbt, boomerang_descriptor, boomlet_0_boom_musig2_pubkey_share, pubnonce_boom} from Boomlet. Iso then generates its own public nonce and sends this pubnonce_normal alongside his partialsignature (partialsig_normal) to Boomlet.
67. Boomlet receives {pubnonce_normal, partialsig_normal} from Iso, proceeds to produce its own partial signature and shares it with Iso. Boomlet saves signed psbt on its own memory.
68. Iso receives partialsig_boom from Boomlet. Iso also keeps signed psbt but informs User that he can connect Boomlet to Niso.
69. User receives {magic: "withdrawal_psbt_signature_created_connect_boomlet_to_niso"} from Iso and proceeds to connect Boomlet to Niso.
70. Niso receives {magic: "withdrawal_peer_is_informed_that_boomlet_should_be_connected_to_niso"} from User and proceeds to ask Boomlet for the signed psbt.
71. Boomlet receives {magic: "withdrawal_signing_finished_export_signed_psbt"} from Niso and exports psbt_signed_by_peer_0. Only after the export operation completes does it clear all active withdrawal state, including the ceremony mystery and retained signed fragment.
72. Niso receives psbt_signed_by_peer_0 from Boomlet and forwards it to WT.
73. WT receives psbt_signed_by_peer_0 from Niso and psbt_signed_by_peer_i [1 <= i <= 4] from Peers. WT verifies that every signed PSBT fragment belongs to the approved withdrawal transaction, aggregates the signed PSBTs, verifies the final transaction tx_id, and relays the resulting tx_signed_by_peers to the network.
